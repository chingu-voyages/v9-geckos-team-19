//takes props from components and map them to create UI
const visualizeData = props => {
  let cityData = this.props.cityData;
  return Object.keys(cityData).map(key => <li> key : myObject[key] </li>);
};

export default visualizedData;
